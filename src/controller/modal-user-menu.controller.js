export default class ModalUserMenu {
  static render() {
    // renderiza Modal Menu do Usuário
    // Botão Editar Perfil deve ter escutador e chamar EditProfile.update()
    // Botão Sair do App deve direcionar para a página de login (index.html)
    // Botão Salvar alterações deve autalizar o hábito serlecionado -> EditHabit(idHabit)
  }
}
